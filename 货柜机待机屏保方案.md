# 货柜机待机屏保方案（轮播图 + 光荣榜）

## 一、目标与范围

- 目标：学生无操作时，货柜机 21.5 寸竖屏（1080×1920，界面 540×960 逻辑像素）自动进入屏保，循环展示「图片轮播」与「光荣榜」，任意触摸立即退出回首页。
- 本期范围：图片轮播 + 光荣榜（学校榜、年级榜、维度榜）。
- 暂不纳入：视频播放、夜间熄屏、防烧屏漂移（均放二期）。

## 二、现状与三个卡点

1. 首页没有空闲计时：现有 `App.tsx` 的空闲逻辑只作用于进入首页之后的页面，`welcome` 被排除在外，触发后做的是「清状态回首页」，不是屏保。
2. 光荣榜数据终端读不到：学生本学期得分目前只是 `components/TeacherDashboard.tsx:434` 的局部 mock（`StudentScoreDetailRow`，12 条），没有共享数据源。
3. 后台配置不落地：`components/TeacherDashboard.tsx:271` 的 `terminalConfig` 是本地 `useState`，既不持久化、终端也不读取。屏保配置必须走共享存储。

## 三、屏保内容与播放模型

屏保是一个**播放列表**，每一项是一种内容块，按顺序循环，每项可单独启停、排序、设停留时长：

| 内容块 | 类型标识 | 说明 |
| --- | --- | --- |
| 图片轮播 | `image` | 每张图一项，可多张 |
| 学校光荣榜 | `honor-school` | 全校本学期总分前 10 |
| 年级光荣榜 | `honor-grade` | 每个年级一页，各前 10，默认覆盖全部年级 |
| 维度光荣榜 | `honor-dimension` | 每个一级指标维度一页，各前 3，维度由学校自定义 |

默认顺序建议：品牌图 → 图片公告 → 学校榜 → 各年级榜 → 各维度榜，循环。

## 四、光荣榜口径

### 1. 数据口径

- 分数口径 = 本学期总净得分（加分 − 扣分），与 PC「学生得分明细表」的 `totalNetScore` 一致。
- 展示文案统一写「本学期总分」，货柜机全线禁用「净得分」字样（`components/GrowthCoinTerminology.test.mjs` 会拦截）。
- 学期范围沿用现有学期起止（demo：`2026-02-17`～`2026-07-05`）。

### 2. 排名表述（与班级标杆的边界）

- 班级内不做任何排名，`积分排行榜规则方案.md` 的「班级标杆」规则保持不变。
- 年级榜、学校榜、维度榜属于**优秀学生展示**，只呈现进入前 10 / 前 3 的学生，不构成对班级内学生的排名。
- 榜单可以显示 1–10 的荣誉序号；不展示「距离下一档差多少」「占全年级百分比」等对比性信息。

### 3. 并列规则

- 同分并列进入，不因人数上限把同分学生截掉。例如第 10 名有 3 人同分，榜单展示到并列的 3 人为止，总人数允许超过 10。
- 无正分、全校或某年级不足 N 人时，展示实际人数；整页无数据时展示明确的空状态。

### 4. 维度榜（不写死五育）

- 维度来源是学校的**一级指标目录**，每个学校可不同，可能 5 个也可能 6 个。现有 demo 一级指标为：崇德、求知、向阳、尚美、躬行、乐创（`mobile-app/data/classReportIndicatorDemo.ts`）。
- 维度榜对**每个一级指标维度**分别取前 3，一个维度一页；维度超过一页容量时自动分页，不做固定「德智体美劳」假设。
- 后台默认维度全开，学校可取消勾选不想展示的维度。

### 5. 榜单字段

- 学校榜 / 年级榜：头像、姓名、班级、本学期总分。
- 维度榜：头像、姓名、班级、该维度分值。
- 头像：使用系统头像库 `mobile-app/assets/studentAvatarCatalog.ts`，按学号稳定映射；接入真实学生档案后再带性别与真实头像。

## 五、终端行为

- 触发：首页 60 秒无操作进入屏保（时长后台可配）。
- 唤醒：任意 `touchstart` / `click` 立即退出回首页，不直接进入刷脸或登录。
- 误触恢复：学生误触退出屏保后，需要能一键重新回到屏保。首页放置一个不显眼的「回到屏保」入口；刷脸页、密码登录弹窗、商品页同样保留该入口，点击后直接重新进入屏保并继续轮播。
- 屏保期间：屏蔽全局点击音效（`App.tsx:723`）、冻结原有 999 秒自动退出计时、不显示「xxs 后自动退出」气泡。
- 数据刷新：每次进入屏保时重算榜单，保证学生兑换后数据不滞后。

## 六、后台配置（组件化）

入口放在 PC 后台「货柜机配置中心 → 终端设备 → 屏幕界面配置」下，新增「待机屏保」配置区。每个内容块对应一个配置组件：

- 总开关与触发时长：是否启用屏保、无操作进入秒数（默认 60）。
- 图片轮播块：增删图片、排序、每张停留秒数、上下线时间。字段参考 `mobile-app/data/teacherCampaigns.ts` 的 `imageUrl / imageAlt / startAt / endAt`。
- 学校榜块：开关 + 停留秒数。
- 年级榜块：开关 + 年级范围（默认全部年级）+ 每页停留秒数。
- 维度榜块：开关 + 勾选参与维度（默认全开，维度来自学校一级指标目录）+ 每页停留秒数。
- 实时预览：用竖屏预览窗实时看效果。

配置数据持久化到共享存储，PC 后台与终端读同一份，改完终端即时生效。

## 七、组件化架构分层

1. 数据层（`shared/`）
   - `terminalScreensaver.ts`：屏保配置模型、读写、更新事件。
   - `studentTermScore.ts`：学生本学期得分共享数据源，PC 报表页与终端共同读取；得分按「维度 id → 分值」动态存储，不再固定五列。
   - `indicatorDimensionCatalog.ts`：学校一级指标维度目录（复用 `TeacherIndicatorCatalog` 的 `levels`），作为维度榜与 PC 得分报表的公共维度来源。
   - `honorRoll.ts`：三个榜单的纯计算函数（排序、并列、前 N、各维度 top3），可单测。
2. 终端渲染层（`components/`）
   - `TerminalScreensaver.tsx`：容器，负责播放循环、计时、唤醒。
   - `ScreensaverImageSlide.tsx`、`HonorRollSchoolBoard.tsx`、`HonorRollGradeBoard.tsx`、`HonorRollDimensionBoard.tsx`。
3. 后台配置层（`components/TeacherDashboard.tsx` 内）
   - `ScreensaverConfigPanel.tsx` 及各内容块配置组件。

## 八、分期实施

- V1：数据层抽取（含维度目录、动态得分）+ 屏保容器 + 图片轮播 + 学校榜 + 后台基础配置。
- V1.5：年级榜、维度榜、并列规则与空状态、PC 得分报表改为动态维度列。
- V2：视频、夜间熄屏、防烧屏漂移。

## 九、已确认与遗留

已确认：
- 年级榜覆盖全部年级。
- 维度榜默认全开，按学校自定义一级指标维度展示，不写死五育。
- 误触进入登录页等页面时提供返回首页入口。

遗留待定：
- 并列规则默认「同分并列全部进入，人数可超上限」，如有异议请指出。
- 维度目录当前 demo 为 6 个一级指标，正式接入时以学校配置为准。

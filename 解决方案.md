# 年级与班级展示方案

## 一、统一名称

### 年级名称

```text
一年级
二年级
三年级
四年级
五年级
六年级
七年级
八年级
九年级
高一
高二
高三
```

### 班级名称

按学校类型展示班级名称：

| 学校类型 | 班级名称示例 |
| --- | --- |
| 小学 | 2026级1班 |
| 初中 | 2026级1班 |
| 高中 | 2026级1班 |
| 九年一贯制 | 小2026级1班、初2026级1班 |
| 十二年一贯制 | 小2026级1班、初2026级1班、高2026级1班 |
| 完全中学 | 初2026级1班、高2026级1班 |

只有九年一贯制、十二年一贯制、完全中学的班级名称带“小”“初”“高”前缀。

## 二、展示范围与输入源

本方案定义年级、班级的名称、排序和展示格式，不代表每个控件都必须展示完整学校范围。各页面、组件和选择器的实际展示范围，以调用方传入的输入源为准。

### 1. 输入源优先

- 调用方应先根据当前账号的权限、页面业务场景和数据状态，过滤出当前控件允许展示的年级或班级，再传入控件。
- 控件只展示输入源中的有效选项，不自行查询、补全或扩展为学校类型对应的全部年级、全部班级。
- 输入源为空时展示对应的空状态或禁用状态，不使用完整年级列表作为兜底数据。
- 年级、班级的名称格式仍遵循本方案；输入源只决定展示哪些项，不改变名称和排序规则。

### 2. “全部年级”的含义

- 只有业务允许汇总当前输入源中的全部年级，且调用方明确开启时，筛选控件才展示“全部年级”。
- “全部年级”表示当前账号、当前页面和当前输入源范围内的全部可见年级，不表示该学校类型的全部年级，也不表示当前账号无权限查看的年级。
- 调用方未提供“全部年级”能力时，只展示输入源中的具体年级；即使学校类型包含多个年级，也不自动增加“全部年级”。

### 3. 年级与班级级联

- 年级选项使用调用方传入的可见年级，并按本方案规定的年级顺序排列。
- 选择年级后，班级选项只展示输入源中属于该年级的可见班级，并按学校正式班级名称展示。
- 没有可见班级的年级，默认不作为可选项展示；如业务需要保留该年级，必须明确展示为空的状态。
- 控件不得通过前端隐藏选项代替权限控制；接口和业务服务仍需对最终提交的年级、班级再次校验。

### 4. 示例

学校类型为十二年一贯制，但当前账号只能看到一年级和七年级时，班级列表的年级筛选只展示：

```text
一年级
七年级
```

如果该筛选控件开启汇总能力，则在上述输入源范围内展示“全部年级”；“全部年级”不应扩展为一年级至九年级、高一至高三。

## 三、教师手机端

### 1. 我的班级

班级卡片展示班级名称、当前年级和学生人数。

小学、初中、高中：

```text
2026级1班
一年级 · 42人
```

九年一贯制、十二年一贯制、完全中学：

```text
小2026级1班
一年级 · 42人
```

第一行展示班级名称，第二行展示当前年级和学生人数。

### 2. 年级筛选

筛选项使用当前年级名称，并以调用方传入的可见年级为实际展示范围。以下列表仅定义各学校类型允许使用的标准年级集合，不要求每个账号或每个页面全部展示：

- 小学：全部年级、一年级至六年级
- 初中：全部年级、七年级至九年级
- 高中：全部年级、高一至高三
- 九年一贯制：全部年级、一年级至九年级
- 十二年一贯制：全部年级、一年级至九年级、高一至高三
- 完全中学：全部年级、七年级至九年级、高一至高三

筛选控件根据输入源和业务能力显示“全部年级”（见“展示范围与输入源”）或具体年级名称。

### 3. 班级来源和班级选择

选择层级：

```text
年级 -> 班级
```

第一层显示当前年级名称，第二层显示班级名称。

示例：

```text
七年级
  初2026级1班
  初2026级2班
```

### 4. 教师个人信息

“分管年级”使用当前年级名称：

```text
一年级至九年级、高一至高三
```

“带班班级”和“任教班级”使用班级选择器，展示具体班级名称。

### 5. 切换班级来源弹窗

学校来源项显示两个标签：

```text
学校 · 学校类型
```

模拟数据：

- 星河实验学校：十二年一贯制
- 成都七中初中附属小学：小学

## 四、其他页面

以下位置统一使用当前年级名称：

- 记录首页的班级信息
- 评价记录的年级筛选和班级筛选
- 班级详情
- 学生列表和学生详情
- 班级报告的年级筛选、统计标题和排名范围
- 班级排行榜的年级筛选和排名范围
- 问卷采集的适用年级
- 档案设计的适用年级
- 学生团队的年级和班级选择
- 作业录入和作业导入的年级、班级信息
- 奖励兑换、奖章发放、人脸更新、兑换密码中的班级信息
- 教师邀请、家长邀请中的班级信息
- 数据报表和大屏中的年级名称

以下位置使用班级名称：

- 班级卡片
- 班级详情标题
- 班级选择结果
- 学生所属班级
- 邀请文案
- 导入导出内容

## 五、统一要求

- 所有页面使用同一套年级名称和排序；实际展示项以当前控件收到的输入源为准。
- 年级筛选、年级统计、年级负责人配置使用当前年级名称，并遵循当前账号和页面的可见范围。
- 班级展示使用学校正式班级名称。
- 年级选择和班级选择统一使用“年级 -> 班级”的选择方式。
- 任何控件不得因学校类型或全量配置而绕过输入源，向当前账号展示不可见的年级或班级。

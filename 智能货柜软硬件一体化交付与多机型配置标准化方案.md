# 校园智能积分兑换终端：智能货柜软硬件一体化交付与多机型配置标准化方案

## 一、 背景与业务痛点
在校园智能积分兑换业务中，自动兑换货柜兼具 **“线下物理硬件”** 与 **“云端 SaaS（软件即服务）平台”** 的双重属性：
1. **采购非标差异**：不同学校、甚至同一学校的不同安装楼层，采购的货柜机型规格存在差异（如：双机柜 49 格、单柜 36 货道、纯格子储物柜等）。
2. **硬件安装调试门槛**：货柜到校安装后，工控机/安卓主板与底层驱动板之间需要调试通信协议、串口设备节点（如 `/dev/ttyS1` 或 `COM1`）、波特率（如 `9600`、`115200`）以及单道电机/电磁锁的动作测试。
3. **运营与工程权限分离**：学校老师只关注奖品上架、库存和积分定价，必须屏蔽底层复杂的工业通信参数；而现场交付工程师与运维人员需要安全、闭环的硬件调试与激活工具。
4. **所见即所得**：PC 管理后台必须呈现与真实设备 1:1 比例吻合的线框布局，杜绝“后台看到的格子排布与眼前的真机对不上”导致的卡货和管理混乱。

---

## 二、 核心架构原则：“B 建立档案（前置配货） + A 握手同步（现场校准）”

为解决“设备未通电前老师无法配货”与“现场改装格口云端无法感知”的矛盾，系统采用混合协同架构：

```
                    ┌────────────────────────────┐
                    │ 阶段 1：采购建档与提前配货 │
                    │    (学校老师 · PC 管理后台) │
                    └─────────────┬──────────────┘
                                  │ 1. 选择采购机型模板（如双机柜 49 格）
                                  │ 2. 系统即刻生成 1:1 物理格口线框图
                                  │ 3. 老师在办公室提前配好奖品与积分规则
                                  │ 4. 系统为该设备生成专属【6位激活码】与【运维密码】
                                  ▼
                    ┌────────────────────────────┐
                    │ 阶段 2：现场安装与硬件调试 │
                    │   (交付工程师 · 货柜机大屏) │
                    └─────────────┬──────────────┘
                                  │ 1. 货柜通电，点击大屏顶部进入维护
                                  │ 2. 输入该设备专属运维密码
                                  │ 3. 配置串口路径、波特率、驱动协议并执行点动测试
                                  ▼
                    ┌────────────────────────────┐
                    │ 阶段 3：双向握手与动态对齐 │
                    │   (终端大屏 ◄──► 云端 SaaS) │
                    └─────────────┬──────────────┘
                                  │ 1. 大屏输入 6 位激活码发起握手
                                  │ 2. 货柜机自动拉取老师已配好的商品与积分
                                  │ 3. 若现场调整了隔板格数，一键将最新拓扑回传云端
                                  ▼
                    ┌────────────────────────────┐
                    │ 阶段 4：日常运营与现场补货 │
                    │    (学生兑换 · 老师补货)   │
                    └────────────────────────────┘
```

---

## 三、 标准数据模型与协议定义

### 1. 设备机型模板库（Preset Model Catalog）
系统预置校园最常用的三款标准柜型，支持一键选型：

| 机型标识（`modelType`） | 中文名称 | 物理组合 | 适用场景 |
| :--- | :--- | :--- | :--- |
| `MODEL_DUAL_49` | **经典双机柜（49 格）** | 左出货柜（30 道推杆/弹簧/挂钩 + 取货口） + 中间主机屏幕 + 右储物柜（19 格多尺寸电磁门） | **旗舰款**：主教学楼大厅、图书馆，兼顾小件文具与大件奖品 |
| `MODEL_SINGLE_36` | **单体出货柜（36 格）** | 单机出货柜（6 排 36 道弹簧/推杆 + 底部大取货口） + 嵌入式触摸屏 | **小空间款**：宿舍楼连廊、楼道转角，专用于标准快消文具 |
| `MODEL_LOCKER_24` | **多门储物柜（24 格）** | 左右双侧纯储物柜（各 12 格独立电磁门，共 24 格大容量） + 中控屏 | **大件款**：体育馆、实验楼，专用于篮球、羽毛球拍、积木盒等大奖品 |

### 2. 柜体物理尺寸（取自出厂三视图）

`MODEL_DUAL_49` 整机 2000mm（宽）× 1850mm（高，不含脚轮）× 800mm（深）：

| 分区 | 宽度 | 排数 | 排高（自柜顶起算，单位 mm） | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| 左柜·出货货道 | 850mm | 6 | 330 / 190 / 180 / 220 / 180 / 210 | 第 1 排挂钩 5 道，第 2 排弹簧 7 道，第 3~6 排推杆 5/5/4/4 道 |
| 左柜·取货口舱 | 850mm | — | 540 | 位于 6 排货道下方，界面按最小可辨认高度渲染 |
| 中柱·主机 | 350mm | — | — | 21.5 寸触控屏，界面按窄条渲染 |
| 右柜·储物格 | 800mm | 5 | 382 / 366 / 334 / 386 / 346 | 第 1~3 排各 2 个大格，第 4 排 10 个窄格，第 5 排 3 个中格 |

线框渲染要求：线框用于核对"屏幕上的格子对应现场哪一格"，因此**排布结构必须与真机一致**（左右柜分区、每排道数、每排机制、取货口位于左柜下方），但**不得为了凑整机比例浪费页面**：取货口舱、中柱等不可配置区域按最小可辨认尺寸渲染，横向空间优先让给格口。格口尺寸一律由所在排的网格分配，不得给单个格口设置固定长宽比或最小高度，否则同一排卡片会互相挤压、与真机排布对不上。

### 3. 机柜拓扑结构化描述（JSON 协议）
设备与云端交互的核心结构，驱动 PC 与终端大屏自适应渲染 1:1 物理线框：

```json
{
  "deviceId": "DEV-2026-F1-01",
  "deviceName": "1号教学楼1层中厅智能柜",
  "modelType": "MODEL_DUAL_49",
  "activationCode": "849201",
  "adminPassword": "Admin@888",
  "hardwareConfig": {
    "port": "/dev/ttyS1",
    "baudRate": 9600,
    "protocol": "MDB_STANDARD",
    "timeoutMs": 3000,
    "status": "online",
    "lastSyncTime": "2026-09-16 11:30:00"
  },
  "cabinets": [
    {
      "id": "cab-left",
      "name": "左柜·出货货道",
      "type": "channel", // 出货柜：前端自动渲染底部通长大取货口
      "rows": [
        { "row": 1, "mechanism": "挂钩", "slots": 5, "heightRatio": "standard" },
        { "row": 2, "mechanism": "弹簧", "slots": 7, "heightRatio": "standard" },
        { "row": 3, "mechanism": "推杆", "slots": 5, "heightRatio": "standard" },
        { "row": 4, "mechanism": "推杆", "slots": 5, "heightRatio": "standard" },
        { "row": 5, "mechanism": "推杆", "slots": 4, "heightRatio": "standard" },
        { "row": 6, "mechanism": "推杆", "slots": 4, "heightRatio": "standard" }
      ]
    },
    {
      "id": "cab-right",
      "name": "右柜·储物格",
      "type": "locker", // 储物柜：前端渲染独立电锁小门
      "rows": [
        { "row": 1, "mechanism": "电锁门", "slots": 2, "heightRatio": "large" },
        { "row": 2, "mechanism": "电锁门", "slots": 2, "heightRatio": "large" },
        { "row": 3, "mechanism": "电锁门", "slots": 2, "heightRatio": "large" },
        { "row": 4, "mechanism": "电锁门", "slots": 10, "heightRatio": "narrow" },
        { "row": 5, "mechanism": "电锁门", "slots": 3, "heightRatio": "medium" }
      ]
    }
  ]
}
```

---

## 四、 终端大屏【终端维护控制台】功能规范

### 1. 访问与鉴权
* **入口**：欢迎页右上角设置图标（或连点左上角 Logo 5 次触发）；
* **鉴权验证**：
  * 支持输入由云端派发的该设备专属【运维密码】（如 `Admin@888`）；
  * 提供工厂离线默认密码（`123456`）用于无网首次开箱调试；
  * 连续输错 5 次锁定 5 分钟，防止学生试探。

### 2. 四大功能 Tab 模块
1. **【硬件调试】（交付工程师）**：
   * **通信配置区**：
     * 串口节点：自动识别或下拉选择系统物理端口（如 `/dev/ttyS0`、`/dev/ttyS1`、`COM1`、`COM3`）；
     * 波特率：下拉选择常用工业速率（`9600`、`19200`、`38400`、`115200`）；
     * 通信协议：下拉选择驱动协议（`MDB售货机协议`、`双向推杆协议`、`多路电控锁协议`）；
     * 操作：一键【保存并测试连通性】。
   * **点动自检区**：
     * 渲染货道与储物格测试按钮，点击对应道（如“测试推杆 1-1”），发送十六进制（HEX）出货旋转指令；
     * 点击储物格（如“测试电锁 2-1”），发送开门脉冲信号；
     * 实时通信日志窗口回显：`TX: 02 01 01 00 04` ➔ `RX: 06 01 00 OK (电机转动完成)`。
2. **【设备激活】（双向绑定握手）**：
   * 输入 PC 端生成的 6 位设备激活码（如 `849201`）；
   * 点击【立即激活绑定】，终端向 SaaS 发送设备身份指纹与硬件状态；
   * 云端验证成功后，自动拉取商品数据并建立实时长连接心跳。
3. **【现场补货】（学校老师/后勤人员）**：
   * 展示与 PC 端完全一致的物理货柜线框图；
   * 提供【一键全开所有储物门】（方便批量放入大件奖品）；
   * 提供【一键全部补满】快捷按钮；
   * 点击单道支持快速标记实物放入。
4. **【系统设置】**：
   * 显示当前局域网 IP、公网连接延迟、固件版本号；
   * 【重启终端程序】与【退出维护控制台】。

---

## 五、 学校 PC 管理后台功能规范

### 1. 【终端设备】模块
* **设备列表**：
  * 字段收敛：设备名称、机型规格标签、摆放位置、**6位激活码**（支持一键复制）、**设备运维密码**（带脱敏眼睛图标查看与重置）、通信状态（在线/离线/串口回显）、配货完成度、操作项（编辑/配置/删除）。
* **新增/编辑设备弹窗**：
  * 设备名称：如“1号教学楼1层中厅机”；
  * **设备机型**：单选卡片/下拉框（经典双机柜49格 / 单体出货柜36格 / 多门储物柜24格），展示结构说明与适用场景；
  * 摆放位置：如“1号教学楼一楼大厅西侧”；
  * 系统自动生成不可重复的 6 位设备激活码与默认强密码。

### 2. 【货柜超市】货道配置模块
* **多机型自适应排布**：
  * 顶部设备切换下拉框选择不同设备时，下方线框组件动态读取该设备的拓扑定义；
  * 若选中“经典双机柜”，渲染左出货柜 + 中屏幕 + 右储物柜；
  * 若选中“单体出货柜”，仅渲染单侧出货柜与底部取货口；
  * 格口点击编辑：绑定商品、设置兑换所需星光币（成长积分）、设定库存上限与单次限制。

---

## 六、 效益与保障
1. **交付效率倍增**：安装工程师现场调试从过去的 1 小时缩短到 5 分钟（开机 ➔ 测串口 ➔ 输激活码 ➔ 完工）。
2. **老师零负担**：学校老师在办公室即可提前排布商品，界面与现场实物 100% 吻合，所见即所得。
3. **高内聚低耦合**：底层工业通信逻辑与上层 SaaS 积分兑换业务彻底解耦，后续支持接入任意品牌各型自动售货柜。
